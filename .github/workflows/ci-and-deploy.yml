name: Verify and deploy

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      update_snapshots:
        description: Generate Linux screenshot baselines for review
        required: false
        default: false
        type: boolean

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: pages-${{ github.ref }}-${{ inputs.update_snapshots || false }}
  cancel-in-progress: true

jobs:
  verify:
    name: Static, unit, rules, and build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: cachix/install-nix-action@v31
      - run: nix develop --command bun install --frozen-lockfile
      - run: nix develop --command bun run check
      - run: nix develop --command bun run check:workflow
      - run: nix develop --command bun run test:unit
      - run: nix develop --command bun run test:rules
      - run: nix develop --command bun run build

  e2e-quarter-1:
    if: inputs.update_snapshots != true
    uses: ./.github/workflows/e2e-quarter-1.yml

  e2e-quarter-2:
    if: inputs.update_snapshots != true
    uses: ./.github/workflows/e2e-quarter-2.yml

  e2e-quarter-3:
    if: inputs.update_snapshots != true
    uses: ./.github/workflows/e2e-quarter-3.yml

  e2e-quarter-4:
    if: inputs.update_snapshots != true
    uses: ./.github/workflows/e2e-quarter-4.yml

  snapshot-baselines:
    if: inputs.update_snapshots == true
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: cachix/install-nix-action@v31
      - run: nix develop --command bun install --frozen-lockfile
      - run: nix develop --command bunx playwright install chromium
      - name: Generate Linux screenshot baselines
        run: nix develop --command bun run test:e2e:update-snapshots
        env:
          CI: 'true'
      - uses: actions/upload-artifact@v4
        with:
          name: linux-screenshot-baselines
          path: tests/e2e/**/screenshots/*.png
          retention-days: 7

  deploy:
    needs: [verify, e2e-quarter-1, e2e-quarter-2, e2e-quarter-3, e2e-quarter-4]
    if: >-
      inputs.update_snapshots != true &&
      needs.verify.result == 'success' &&
      needs.e2e-quarter-1.result == 'success' &&
      needs.e2e-quarter-2.result == 'success' &&
      needs.e2e-quarter-3.result == 'success' &&
      needs.e2e-quarter-4.result == 'success' &&
      (github.event_name != 'pull_request' ||
      github.event.pull_request.head.repo.full_name == github.repository
      )
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: cachix/install-nix-action@v31
      - run: nix develop --command bun install --frozen-lockfile
      - name: Determine deployment path
        id: deployment
        env:
          EVENT_NAME: ${{ github.event_name }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: nix develop --command bash scripts/deployment-path.sh
      - name: Build static client
        run: nix develop --command bun run build
        env:
          PUBLIC_BASE_PATH: ${{ steps.deployment.outputs.base }}
          VITE_GIT_HASH: ${{ github.event.pull_request.head.sha || github.sha }}
          VITE_FIREBASE_API_KEY: ${{ secrets.VITE_FIREBASE_API_KEY }}
          VITE_FIREBASE_AUTH_DOMAIN: ${{ secrets.VITE_FIREBASE_AUTH_DOMAIN }}
          VITE_FIREBASE_PROJECT_ID: ${{ secrets.VITE_FIREBASE_PROJECT_ID }}
          VITE_FIREBASE_STORAGE_BUCKET: ${{ secrets.VITE_FIREBASE_STORAGE_BUCKET }}
          VITE_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.VITE_FIREBASE_MESSAGING_SENDER_ID }}
          VITE_FIREBASE_APP_ID: ${{ secrets.VITE_FIREBASE_APP_ID }}
      - uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: build
          destination_dir: ${{ steps.deployment.outputs.destination }}
          keep_files: true
      - name: Link the retained pull-request preview
        if: github.event_name == 'pull_request'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          PREVIEW_URL: >-
            https://${{ github.repository_owner }}.github.io/roborally/pr${{
            github.event.pull_request.number }}/
        run: >-
          nix develop --command gh pr comment "$PR_NUMBER"
          --repo "$GITHUB_REPOSITORY"
          --edit-last
          --create-if-none
          --body "Preview: [$PREVIEW_URL]($PREVIEW_URL)"
